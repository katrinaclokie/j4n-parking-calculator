apply plugin: "java"
apply plugin: "eclipse"

apply from: "repositories.gradle", to: repositories
apply from: "dependencies.gradle"

sourceSets {
    main {
        java {
            srcDirs "src/main"
        }
    }
    test {
        java {
            srcDirs "src/test"
        }
    }
}